{
  "name": "shopping-site-backend",
  "version": "1.0.0",
  "description": "Backend for a simple shopping site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"  # For development; install nodemon as a dev dependency
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",  # Web framework for Node.js
    "body-parser": "^1.20.2"  # Middleware to parse JSON bodies
  },
  "devDependencies": {
    "nodemon": "^3.0.1"  # Auto-restarts server on changes
  }
}
